{
  "devDependencies": {
    "autoprefixer": "^10.3.3",
    "postcss": "^8.3.6",
    "tailwindcss": "^2.2.8"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.3.3"
  },
  "scripts": {
    "dev:tailwind": "tailwindcss -i app.css -o compiled.css --watch --minify",
    "build:tailwind": "NODE_ENV=production tailwindcss -i app.css -o compiled.css --watch --minify"
  }  
}
